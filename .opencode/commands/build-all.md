---
description: Compila RIVAREN para Linux + Windows + macOS en paralelo.
agent: build
---
Ejecuta la compilación multiplataforma y reporta resultado por target:

!`cargo xtask build-all --release`

Si falla un target, muestra el error resumido y propone el fix.
